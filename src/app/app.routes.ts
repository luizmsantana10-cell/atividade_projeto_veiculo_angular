import { Routes } from '@angular/router';
import { ListaAutomoveisComponent } from './lista-automoveis/lista-automoveis.component';

export const routes: Routes = [
{
    path:'',
    component: ListaAutomoveisComponent
}    
];
